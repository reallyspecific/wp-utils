{
	"name": "wp-utils",
	"version": "1.0.0",
	"license": "GPL-3.0-or-later",
	"type": "module",
	"scripts": {
		"assets:build": "bodykit --build=all --in=assets",
		"assets:watch": "bodykit --watch --in=assets"
	},
	"devDependencies": {
		"bodykit": "git+https://github.com/reallyspecific/bodykit.git"
	}
}
